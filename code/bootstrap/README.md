the ansible playbook assumes:
1) paru are already installed
2) since its stored in chezmoi repo need at least git but should have cheazmoi installed as well
3) also require ansible to run

the bootstrap script:
1) assumes paru, it, and chezmoi are installed
